{
  "name": "examplelibrary",
  "version": "1.0.0",
  "main": "lib/index.js",
  "types": "lib/index.d.ts",
  "dependencies": {},
  "devDependencies": {
    "typescript": "^3.9.6"
  },
  "engines": {
    "node": ">=5.0.0"
  },
  "scripts": {
    "all": "npm run compile",
    "clean": "rm -fR node_modules && rm -f lib/*",
    "compile": "./node_modules/.bin/tsc"
  }
}
